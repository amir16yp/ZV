// kernel32_handle.zv
//
// Auto-generated ZV bindings from handleapi.h
// DLL: kernel32.dll
//
// Review before use: unsupported parameter types and skipped functions
// are reported below in the generation warnings.

#include "types.zv"

extern "kernel32.dll" {
    BOOL32 CloseHandle(HANDLE hObject);
    BOOL32 DuplicateHandle(HANDLE hSourceProcessHandle, HANDLE hSourceHandle, HANDLE hTargetProcessHandle, PTR<HANDLE> lpTargetHandle, DWORD dwDesiredAccess, BOOL32 bInheritHandle, DWORD dwOptions);
    BOOL32 CompareObjectHandles(HANDLE hFirstObjectHandle, HANDLE hSecondObjectHandle);
    BOOL32 GetHandleInformation(HANDLE hObject, PTR<DWORD> lpdwFlags);
    BOOL32 SetHandleInformation(HANDLE hObject, DWORD dwMask, DWORD dwFlags);
}